/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

/*import { PatientExternalDialogComponent } from './patient-external-dialog.component';#1#

describe('PatientExternalDialogComponent', () => {
  let component: PatientExternalDialogComponent;
  let fixture: ComponentFixture<PatientExternalDialogComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PatientExternalDialogComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PatientExternalDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
